/******************************************************************************/
class Player
{
   Vec2 pos(0, 0);

   void move(C Vec2 &dir)
   {
      pos+=dir*Time.d()/4; Clamp(pos.x, -0.25, 0.25); Clamp(pos.y, -0.25, 0.25);
   }

   void draw(C Rect &map_rect)
   {
      (pos+map_rect.center()).draw(GREEN);
   }
}
/******************************************************************************/
Player Plr;
/******************************************************************************/
